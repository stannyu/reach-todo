import React from "react";
import { Link, Outlet } from "react-router-dom";

const BoardComponent = (): React.ReactElement => {
  return (
    <div>
      Board groups
      {/* <Outlet /> */}
    </div>
  );
};

export default BoardComponent;
